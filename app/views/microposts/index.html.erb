<h1>Microposts#index</h1>
<p>Find me in app/views/microposts/index.html.erb</p>
<title><%= @title %> Post a tip</title>
<p><%= link_to "Back to List", microposts_path %></p>


<%= render 'form' %>
<%= %>


<table>
  <tr>
    <th> Tellmewhatyouthink #</th>
    <th> content </th>
    <th> tag </th>
  </tr>
  <%  for micropost in @microposts %>
   <tr>
      <td><%= micropost.id %></td>
      <td><%= micropost.content %></td>
      <td><%= micropost.tag %></td>
      <td><%= link_to "Show", micropost %></td>
      <td><%= link_to "Edit", edit_micropost_path(micropost) %></td>
      <td><%= link_to "Destroy", micropost, :confirm => 'Are you sure?', :method => :delete  %></td>
   <% end %>
   </tr>
</table>
<p><%=  link_to "New micropost", new_micropost_path %></p>
